<!DOCTYPE html>
<html>
  <head>
    <title><%=title %></title>
  </head>
<body>
  <h1><%=title %></h1>
  <br></br>
  <h2>Lookie lookie </h2>
  id: <%= product.map(product => product.id) %>
  <br>
  name: <%= product.map(product => product.product) %>
  <br>
  category: <%= product.map(product => product.category) %>
  <br>
  stock:  <%= product.map(product => product.stock) %>
 
</body>
</html>